spring:
  application:
    name: notification
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

logging:
  level:
     org.springframework.security: debug

