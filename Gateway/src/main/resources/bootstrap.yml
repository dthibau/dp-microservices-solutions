spring:
  application:
    name: Gateway

        
  cloud:
    config:
      uri:
      - http://config:8888
      fail-fast: true
      
      
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
